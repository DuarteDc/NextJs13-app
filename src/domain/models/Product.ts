export class Product {
 
    constructor(readonly _id: string, readonly name: string, readonly slug: string, readonly price: number, readonly purchase_price: number, readonly discount: number) { }
    
}